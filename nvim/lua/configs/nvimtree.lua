local options = {
  disable_netrw = true,
  hijack_cursor = true,
  sync_root_with_cwd = true,
  update_focused_file = {
    enable = true,
  },
  view = {
    preserve_window_proportions = true,
  },
  live_filter = {
    always_show_folders = false,
  },
}

return options
